import { ExactDatePipePipe } from './exact-date-pipe-pipe';

describe('ExactDatePipePipe', () => {
  it('create an instance', () => {
    const pipe = new ExactDatePipePipe();
    expect(pipe).toBeTruthy();
  });
});
